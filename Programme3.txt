.include beta.uasm
.include intio.uasm
.options tty
	CMOVE(pile,SP)
	BR(main)
data:
x:	LONG(0)	| int x;
a:	LONG(100)	| int a;
b:	LONG(170)	| int b;
main:
	PUSH(BP);
	PUSH(BP);
	MOVE(SP, BP);
	PUSH(BP);
	MOVE(SP, BP);
	POP(R1)
	POP(R2)
	ADD(R1,R2,R0)
	PUSH(R0)
	POP(R1)
	POP(R2)
	ADD(R1,R2,R0)
	PUSH(R0)
}
pile :
