.include beta.uasm
.include intio.uasm
.options tty
	CMOVE(pile,SP)
	BR(main)
data:
i:	LONG(null)	| int i;
main:
	PUSH(BP);
	PUSH(BP);
	MOVE(SP, BP);
	LDR(0, Rc);
	CMP(R1,R2)
TANT_QUE_84 :
	PUSH(BP);
	MOVE(SP, BP);
	POP(R1)
	POP(R2)
	CMP(R1,R2)
	JL 84
JMP INSTRUCTIONS_84
INSTRUCTIONS_84 :
	POP(R0)
	WRINT()
	PUSH(BP);
	MOVE(SP, BP);
	POP(R1)
	POP(R2)
	ADD(R1,R2,R0)
	PUSH(R0)
}
pile :
